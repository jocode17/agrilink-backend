[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["dotnet restore AgriLink.API/AgriLink.API.csproj"]

[phases.build]
cmds = ["dotnet publish AgriLink.API/AgriLink.API.csproj -c Release -o out"]

[start]
cmd = "dotnet out/AgriLink.API.dll"